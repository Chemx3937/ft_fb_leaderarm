# FT-20260819-01: final13 모델 최대 force-vector 오차 1 N 초과

- 상태: OPEN
- 분류: MODEL
- 누가: PC offline `ft_free_space_train`, 오른팔 physical-AFT dataset
- 언제: 2026-08-19 06:03 KST 학습 완료
- 어디서: `/home/vision/.ros/ft_fb_leaderarm/datasets/right_final13_20260819`,
  `/home/vision/.ros/ft_fb_leaderarm/models/right_final13_20260819`
- 무엇을: validation으로 선택한 `dynamic_mlp`의 force-vector 최대 오차가
  validation `3.980 N`, held-out test `6.260 N`으로 `1 N` 기준을 넘었다.
- 왜: 직접 관찰된 원인은 독립 zero-set/궤적에서 모델 residual이 남는 것이다.
  tare·시간·cable/구조 하중 변화, 현재 `[q,dq,qdd]` 입력으로 설명되지 않는 AFT
  변동과 모델 표현력의 기여도는 아직 분리되지 않았다.
- 어떻게: task 3개와 diverse 10개를 13개 독립 zero group으로 결합하고 seed 7로
  train/validation/test를 `7/3/3` group으로 분리했다. 5개 ablation을 CPU에서
  최대 60 epoch 학습하고 validation 최대 오차로 하나를 선택한 뒤, 선택 모델만
  held-out test와 2,000회 CPU runtime benchmark에 사용했다.

## 실행 조건

- source commit: `58766238b78ff06fd273d5ec5c084bd9c306b61b`, clean worktree
- dataset: 13 episodes, 13 zero groups, 248,791 samples, 947.713초
- split: train/validation/test `7/3/3` groups, seed `7`
- payload: `right_tool_m2p1kg_oz0p17m_v1`
- controller: `bae_r_v2_c113eabf7e13_ca07ae197213`
- FT/observer frame: `aft_sensor2` / `right_base_link`
- model SHA-256: `b6caba8ce15d0923845d6b1a18340e02b90ea5cf0c89d3e14f0dec8f0eb251a8`
- report SHA-256: `865cc79a652aa5a681bf97585a3bb0dc7be957e9d9bc09f229e255c22ad13b55`
- training: CPU, epochs `60`, batch `1024`, learning rate `0.001`,
  group당 최대 `20,000` windows
- hardware: offline 학습이므로 robot/AFT/hand driver 미사용, 이동·zero-set 없음

## Validation 모델 선택

선택 우선순위는 validation force norm의 max, p95, RMSE 순이다.

| 후보 | best epoch | force max [N] | p95 [N] | RMSE [N] |
|---|---:|---:|---:|---:|
| static linear | 58 | 5.793 | 2.565 | 1.257 |
| dynamic MLP | 27 | **3.980** | 1.687 | 0.951 |
| history MLP | 41 | 4.409 | 1.696 | 0.912 |
| history LSTM | 17 | 4.871 | **1.624** | **0.882** |
| history GRU | 13 | 5.054 | 1.661 | 0.895 |

`dynamic_mlp`가 validation 최대 오차가 가장 낮아 선택됐다. test 결과는 이 선택에
사용하지 않았다.

## 선택 모델 held-out test

| 평가 | samples | force max [N] | p95 [N] | RMSE [N] | 1 N gate |
|---|---:|---:|---:|---:|---|
| validation 3 groups | 58,074 | 3.980 | 1.687 | 0.951 | FAIL |
| held-out test 3 groups | 86,506 | 6.260 | 1.460 | 0.857 | FAIL |

Test force 축별 최대 절대 오차는 `[4.377,4.421,5.012] N`이다.

| held-out group | 주 궤적 | force max [N] | p95 [N] | RMSE [N] |
|---|---|---:|---:|---:|
| `div01` | 좌우/J1 | 2.691 | 1.317 | 0.780 |
| `div08` | 대각선·3D 곡선 | 6.260 | 1.547 | 0.896 |
| `div10` | task 변형 | 4.980 | 1.445 | 0.854 |

모든 held-out group의 p95가 `1 N`을 넘으므로 단일 최대 outlier만의 문제는 아니다.

## CPU runtime

- device: CPU
- calls: 2,000
- mean latency 등가 처리율: 약 `47,100 Hz` (`0.02122 ms`)
- p99 latency 등가 처리율: 약 `41,900 Hz` (`0.02388 ms`, PASS)
- 최악 latency 등가 처리율: 약 `32,400 Hz` (`0.03088 ms`, PASS)
- target period: `3.80952 ms`, required rate `262.5 Hz`
- 전체 학습·평가 wall time: `396.14초`

위 처리율은 model-only latency의 역수이며 실제 ROS publish rate가 아니다.

## Artifact

- clean dataset:
  `/home/vision/.ros/ft_fb_leaderarm/datasets/right_final13_20260819/`
- dataset report:
  `/home/vision/.ros/ft_fb_leaderarm/datasets/right_final13_20260819/dataset_validation_v1.json`
- ablation report:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_final13_20260819/ablation_report.json`
- rejected model metadata:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_final13_20260819/metadata.json`
- diagnostic model:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_final13_20260819/model.ts`

## 원인 분석과 조치

관찰:

- dataset 계약과 split은 통과했고 모델 runtime도 충분하다.
- validation 3개와 test 3개 독립 group 모두 p95 `1 N`을 넘었다.
- RMSE는 validation/test 모두 `1 N` 아래지만 합격 기준은 최대 오차 `1 N`이다.
- history 모델은 RMSE를 줄였으나 최대 오차를 줄이지 못했다.
- 앞선 task-only pilot도 validation/test 최대 `4.295/2.733 N`으로 실패했다.

조치:

- `metadata.approved=false`를 유지하고 diagnostic `model.ts`를 runtime에 배포하지
  않는다. `BundlePredictor`가 실제로 rejected model load를 거부함을 확인했다.
- held-out test는 이미 한 번 사용했으므로 다음 모델 변경의 선택 기준으로 재사용하지
  않는다. 변경 모델의 최종 판정에는 새 독립 zero-set test data가 필요하다.
- validation residual을 zero group, 자세, 속도와 시간에 대해 분리한 후속 분석을
  아래에 추가했다.
- 정확도 gate가 실패했으므로 observer-only 실기와 feedback 단계로 승격하지 않는다.

## 2026-08-19 residual 후속 분석

- task 3개는 모두 final13 train에 포함됐고 max/p95/RMSE는
  `3.903/1.303/0.743 N`이었다. 이는 in-sample이라 일반화 evidence가 아니다.
- diverse validation 3개는 `3.980/1.687/0.951 N`, 이미 공개된 test 3개는
  `6.260/1.460/0.857 N`이었다.
- episode 평균 residual norm은 `0.050~0.616 N`이었지만, 평균 residual을 완벽히
  제거하는 oracle도 전체 p95를 `1.436 → 1.357 N`만 낮추고 max는 낮추지 못했다.
  상수 offset은 전체 force squared error의 약 `11.9%`였다.
- 정지에 가까운 구간의 max/p95/RMSE는 `2.360/1.014/0.581 N`, 이동 구간은
  `6.260/1.492/0.865 N`이었다. 속도와 가속도 사분위가 증가할수록 p95가 단조롭게
  증가했다.
- `div08` 최대 오차 시 acceleration norm은 `9.059 rad/s²`였고 test 최대값은
  train 최대 `8.399 rad/s²`를 넘는 `15.664 rad/s²`였다. 새 동적 상태의 범위
  부족이 일부 기여하지만, train 범위 안인 `div01`도 max `2.691 N`이어서 유일한
  원인은 아니다.
- validation에서 `-16~+16 samples` global lag를 탐색해도 RMSE 개선은
  `0.9511 → 0.9499 N`뿐이고 max는 악화되어 단순 timestamp shift 가설은
  지지되지 않았다.

판정은 상수 offset/drift만의 문제가 아니라 동적 구간 residual과 상태 범위 문제가
더 크게 남아 있다는 것이다. warm-up/zero 절차는 유지한다. causal acceleration
smoothing과 짧은 history의 validation 평가는 아래 후속 실험에서 완료했다. 상세
지표 해석과 task/diverse 표는
[수집 실험 기록](../experiment/free_space_wrench_dataset_collection.md#평가지표-의미와-해석-방법)에
기록했다.

## 2026-08-19 feature·ensemble·data-size ablation

- 기존 5개에 qdd-smoothed MLP와 8-sample history MLP를 추가했지만 단일 모델의
  validation 최선 max는 `3.947 N`으로 실패했다.
- dynamic MLP와 8-sample history MLP의 단순 평균이 max를 `3.530 N`까지 낮췄지만
  residual 상관이 `0.726`이고 `1 N` 기준을 통과하지 못했다.
- train groups를 `3→5→7`로 늘리자 같은 validation의 max가
  `11.168→4.703→3.980 N`으로 감소했다. 추가 데이터는 도움이 되지만 궤적 coverage도
  함께 바뀐 결과라 sample 개수만의 효과는 아니다.
- 새 후보와 ensemble은 test에 사용하지 않았고 runtime source에도 추가하지 않았다.
- 다음 조치는 기존 validation을 보존하고 `div02/div05/div07` 및 높은 가감속 범위를
  겨냥한 새 독립 zero-set train 6 groups를 수집하는 것이다.

전체 결과와 처리율은
[feature ablation 실험](../experiment/free_space_wrench_feature_ablation.md)에 기록했다.

## 2026-08-21 targeted6 수집 체크포인트 (완료)

이 절이 다음 작업의 기준이다. 새 대화나 장비 재기동 뒤에는 이전 terminal 상태를
추측하지 말고 여기서부터 확인한다.

### 현재 상태

- 완료: task 3개, diverse 10개 수집과 final13 validation/test 평가
- 완료: residual, qdd smoothing, 8-sample history, equal-weight ensemble,
  train group `3/5/7` learning curve 분석
- 현재 최선 validation max: ensemble `3.530 N`; 목표 `1 N` 실패
- 승인 모델: 없음, `metadata.approved=false`
- 금지: observer-only, contact 판정, feedback 단계로 승격하지 않는다.
- 다음 작업: 누락된 동적 범위를 겨냥한 train episode 6개 수집

### 기존 split 보호

다음 학습에서 seed로 split을 새로 섞지 않고 아래 역할을 고정한다.

| 역할 | 기존 zero-set IDs | 사용 규칙 |
|---|---|---|
| train | `tare_20260819_02`, `tare_20260819_03`, `tare_20260819_04`, `tare_20260819_div03`, `tare_20260819_div04`, `tare_20260819_div06`, `tare_20260819_div09` | 새 targeted 6개를 여기에만 추가 |
| validation | `tare_20260819_div02`, `tare_20260819_div05`, `tare_20260819_div07` | 모델·feature·ensemble 선택에 그대로 사용 |
| 과거 test | `tare_20260819_div01`, `tare_20260819_div08`, `tare_20260819_div10` | 이미 공개됐으므로 보관만 하고 새 선택·최종 판정에 사용 금지 |

원본 final13과 기존 report는 수정하거나 덮어쓰지 않는다. 새 수집 권장 경로와 ID는
다음과 같다.

```text
dataset: /home/vision/.ros/ft_fb_leaderarm/datasets/right_targeted6_YYYYMMDD/
zero IDs: tare_YYYYMMDD_target01 ... tare_YYYYMMDD_target06
```

### 수집할 6개 episode

| 순서 | zero-set ID suffix | 무접촉 궤적 | 목표 시간 |
|---:|---|---|---:|
| 1 | `target01` | near/far, J2/J3 중심, 느림·보통 속도 | 60~90초 |
| 2 | `target02` | near/far, J2/J3 중심, 완만한 속도·가감속 변화 | 60~90초 |
| 3 | `target03` | Cartesian X 양방향 왕복 | 60~90초 |
| 4 | `target04` | Cartesian Z 양방향 왕복 | 60~90초 |
| 5 | `target05` | 3D 대각선·곡선, 양방향 | 60~90초 |
| 6 | `target06` | 3D 곡선과 안전한 범위의 빠른 가속·감속 | 60~90초 |

한 episode에서 표의 다른 역할을 모두 섞지 않는다. 시작·종료 정지 구간을 포함하고,
물체·테이블·케이블과 접촉하거나 cable strain이 생기면 해당 episode를 폐기한다.

### 다음 장비 session 시작 조건

1. AFT 전원을 새로 켰다면 formal 수집 전 120분 warm-up한다. 전원을 끄지 않았고 이미
   warm 상태라면 전원 ON 경과시간을 먼저 기록하고 cold-start warm-up을 반복하지
   않는다.
2. robot driver, 일반 V2 impedance controller, hand driver, AFT driver와 통신 상태를
   새로 확인한다. 이전 session process가 살아 있다고 가정하지 않는다.
3. 기준 자세 `[5.5, 52.0, 112.0, 28.0, -107.0, -35.0] deg`, 무접촉·정지를 확인한다.
4. 매 episode 직전에 기준 자세에서 서로 다른 hardware zero-set을 수행한다.
5. [수집 실험의 Collector/GUI 절](../experiment/free_space_wrench_dataset_collection.md#collectorgui와-episode)에
   따라 `CURRENT/SLOW`는 정렬에만 쓰고 `FAST`만 저장한다.
6. 로봇과 leader 입력은 운용자가 직접 수행한다. 에이전트는 명시적 승인 없이 이동,
   zero-set 또는 수집 명령을 실행하지 않는다.

### 6개 수집 완료 후 에이전트가 할 일

1. 새 6개 NPZ/JSON의 CRC, metadata, rate, gap, sync rejection과 무접촉 여부를 검증한다.
2. 기존 train 7개와 새 targeted 6개만 결합한 train manifest를 만든다.
3. 기존 validation `div02/05/07`만 사용해 dynamic MLP, qdd smoothing, short history와
   제한된 ensemble을 다시 비교한다. 과거 test는 읽지 않는다.
4. validation max가 `<= 1 N`이면 방법을 고정하고 다른 날짜·전원 cycle에서 새
   held-out test를 최소 3개 수집한다.
5. max가 `1~3 N`이면 group별 residual을 보고 추가 targeted batch 여부를 한 번만
   결정한다. max가 `>= 3 N`이면 데이터 수집을 중단하고 sync, payload·중력 모델과
   target noise 한계를 다시 분리한다.

다음 대화에서 사용할 재개 문구:

```text
FT-20260819-01의 "다음 session 재개 체크포인트"부터 이어서 진행해줘.
현재 AFT 전원 ON 경과시간은 ___이고 targeted6 수집은 ___/6 완료 상태야.
```

## 2026-08-21 targeted6 고정 validation 결과

### 수집과 고정 split

- AFT cold power-on 뒤 120분 warm-up, 매 episode 독립 hardware zero-set, 동일
  payload/controller/frame 계약으로 targeted 6개를 수집했다.
- 채택한 새 파일은 `170754`, `171634`, `174146`, `175737`, `180730`,
  `181611`이다. J3 limit가 발생한 `172434`와 역할이 중복된 `175055`는 제외했다.
- 고정 manifest는 기존 train 7개와 새 targeted 6개를 train으로, 기존
  `div02/div05/div07`만 validation으로 사용한다.
- 공개된 과거 test `div01/div08/div10`은 로드하지 않았다. 새 held-out test도
  validation 실패 때문에 수집하거나 평가하지 않았다.

Artifact:

- manifest:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_train13_targeted6_diagnostic_v4_20260821/split_manifest.json`
- validation report:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_train13_targeted6_diagnostic_v4_20260821/validation_report.json`
- 실행 script: `scripts/run_targeted6_validation_ablation.py`
- SHA-256: manifest
  `be7f8cfb440bf1fec992c0785e5a7a2b86611c98128809563ad9d1ea63bba8da`, report
  `d940b246a9b4a2f35c9df18f6f8130567cfe3ce95f8ef1b0dfbc879c460421fd`, script
  `b3ffe9be83b87846b9306a3a77ff432b9e8c57cb36ab70da75847f308f607563`

### 후보 비교

동일 train13/validation3, 최대 60 epoch, group당 최대 20,000 windows 조건이다.
선택 순서는 validation force max, p95, RMSE다.

| 후보 | max [N] | p95 [N] | RMSE [N] | 판정 |
|---|---:|---:|---:|---|
| static linear | 5.971 | 2.572 | 1.424 | FAIL |
| dynamic MLP | 3.884 | 1.613 | 0.923 | FAIL |
| qdd-smoothed MLP | **3.201** | 1.372 | 0.788 | FAIL |
| causal lag-2 qdd-smoothed MLP | 3.539 | **1.286** | **0.752** | FAIL |
| short-history MLP, 8 samples | 3.781 | 1.349 | 0.769 | FAIL |
| dynamic + short-history 평균 | 3.531 | 1.403 | 0.802 | FAIL |

qdd-smoothed MLP가 max 기준 최선이지만 `3.201 N`으로 `1 N` gate를 실패했다.
validation group별 max/p95/RMSE는 `div02`가 `3.201/1.698/0.915 N`, `div05`가
`2.728/1.334/0.841 N`, `div07`이 `2.069/1.022/0.607 N`이다. 새 model bundle은
승인하거나 runtime에 배포하지 않았다.

### 실제 task 3개 in-sample 평가

`tare_20260819_02/03/04`는 동일 모방학습 task를 반복한 궤적이며 모두 train13에
포함된다. 아래 값은 배포 성능이나 일반화 evidence가 아니라 학습 궤적 적합도다.

| 후보 | samples | max [N] | p95 [N] | RMSE [N] | 판정 |
|---|---:|---:|---:|---:|---|
| static linear | 15,471 | 4.699 | 1.915 | 1.195 | FAIL |
| dynamic MLP | 15,471 | 4.731 | 1.519 | 0.845 | FAIL |
| qdd-smoothed MLP | 15,450 | 4.476 | 1.285 | 0.733 | FAIL |
| causal lag-2 qdd-smoothed MLP | 15,444 | **3.884** | 1.162 | 0.685 | FAIL |
| short-history MLP, 8 samples | 15,450 | 4.005 | **1.146** | **0.664** | FAIL |
| dynamic + short-history 평균 | 15,450 | 4.356 | 1.235 | 0.705 | FAIL |

validation으로 선택된 qdd-smoothed MLP의 task별 max/p95/RMSE는 task02 ID
`tare_20260819_02`가 `4.476/1.421/0.793 N`, task03 ID `tare_20260819_03`이
`2.286/1.327/0.763 N`, task04 ID `tare_20260819_04`가
`2.000/1.094/0.645 N`이다. 실제 task in-sample에서도 모든 후보의 max가 `1 N`을
넘었고 첫 task 반복이 전체 max를 지배했다.

### sync, payload·중력, target 반복성 분리

Sync 관찰:

- 선택 모델 prediction을 사후 +2 samples, 약 7.6 ms 이동하면 max/RMSE가
  `3.201/0.788 → 2.890/0.753 N`으로 낮아졌다.
- 그러나 같은 정렬을 causal feature로 다시 학습한 lag-2 후보는 max가
  `3.539 N`으로 악화됐다. short-history 후보의 최적 lag는 0이고 dynamic MLP는
  +2에서 RMSE만 소폭 개선되며 max는 `3.884 → 3.936 N`으로 악화됐다.
- force error와 절대 sync error의 상관은 `0.039`였다. 공통 timestamp shift가
  최대오차의 주원인이라는 가설은 지지되지 않는다.

Payload·중력과 동적 항 관찰:

- controller 설정의 `use_ft_payload_gravity_comp`는 `false`다. `payload_id`는
  dataset 혼합 방지 식별자이며 실제 중력 보정을 설정하지 않는다.
- 자세만 쓰는 static gravity baseline max는 `5.971 N`이다. 선택 모델도 가장 낮은
  acceleration 사분위에서 max/p95 `2.198/0.975 N`, 가장 높은 사분위에서
  `3.109/1.655 N`이었다. 동적 항이 오차를 키우지만 quasi-static 구간도 max gate를
  통과하지 못한다.
- validation group별 상수 residual을 oracle로 제거해도 max는
  `3.201 → 3.087 N`만 감소했다. tare offset만으로 최대오차를 설명할 수 없다.

### 2026-08-21 물리 payload/frame 계약 재확인

운영자가 제공한 장착 사진, AIDIN 공식 자료, 제조사 hand URDF와 기존
train13을 교차 확인했다. 사진의 센서는 `AFT200-D80-C`이며 hand와
센서 사이의 검은색 hand-side adapter가 식별된다.

- [AIDIN-Hand 공식 brochure](https://www.aidinrobotics.co.kr/en/_files/ugd/ba4131_12be56ce54fb4486bc2b67aba2cbef13.pdf?index=true)는
  hand 단품 무게 `1.3 kg`, 크기 `291 x 112 x 120 mm`를 명시하지만
  질량중심 좌표는 제공하지 않는다.
- [AFT200 공식 manual](https://help.aidinrobotics.co.kr/en/6-axis-force-torque-sensor-can)의
  센서 크기는 `D80 x H21.5 mm`이며, [공식 drawing](https://help.aidinrobotics.co.kr/hubfs/%EC%97%90%EC%9D%B4%EB%94%98%20%EB%8F%84%EB%A9%B4/AFT200_D80_Drawing.pdf?hsLang=en)은
  sensing reference origin이 장착면과 다른 위치에 있음을 보여준다.
- 현재 URDF의 `right_hand_base_link` 아래 21개 link 질량 합은
  `1.1331013 kg`이다. zero hand joint에서 composite CoM은 hand-base 기준
  `[-0.002164, +0.009990, +0.107435] m`이다. 공식 hand 무게와도
  `0.1669 kg` 차이가 있고 sensor·adapter 장착 변환은 URDF에 없다.
- 운영자 확인과 사진의 X/Y 표시를 기준으로 AFT 센서와
  `right_link_6` 축 회전은 동일하다. 회전 교정값은 `[0, 0, 0]`을 유지한다.
- 다만 sensing origin과 `right_link_6` 원점은 동일하지 않다. 사진만으로
  robot-side plate 두께와 부호를 mm 단위로 확정할 수 없으므로 이동값은
  아직 미확정으로 남겨둔다.

운영자 후속 확인 결과, hand-side 검은색 adapter의 모델명과 실제 질량은
확인되지 않았고 AFT load-side 면부터 hand 장착면까지의 stack은 약 `30 mm`로
추정된다. 이 값은 자/도면으로 실측한 값이 아니므로 URDF나 runtime frame의
확정 이동값으로 사용하지 않는다. AFT·hand cable은 충분한 slack을 두고 로봇 쪽에
Velcro로 고정해 sensor에 당김이 전달되지 않도록 정리했다. 전체 cable 길이를 한
사진에 담는 것은 필수 evidence로 요구하지 않으며, 이후 측정 중 sensor 근처 cable을
만지거나 배치를 바꾸지 않는 것을 운용 계약으로 둔다.

train13만으로 `F=mΔg`, `M=r×F`를 적합한 물리 식별 결과는 다음과 같다.
식별에 validation은 사용하지 않았고 가속도 하위 25% sample을 사용했다.
moment design self-check에서 `r×F`와 반대인 초기 일회성 계산의 행렬 부호를
발견했고, 아래 CoM은 세 축 부호를 교정한 값이다. 질량과 force metric은
이 부호 교정의 영향을 받지 않는다.

| 물리량 | 기존 가정/설정 | 확인·식별값 | 수정 판정 |
|---|---:|---:|---|
| hand 단품 질량 | - | `1.3 kg` | 공식 spec 확정 |
| hand-side adapter 모델/질량 | 미반영 | 미확인 | 설정값으로 사용 금지 |
| AFT load-side → hand 장착면 stack | zero | 약 `30 mm` | 참고치; 실측 전 적용 금지 |
| AFT 아래 유효 payload 질량 | `2.1 kg` payload ID | `1.634 kg` | 진단 초기값; adapter 포함 |
| 유효 CoM, AFT frame | `z=+0.17 m` ID 가정 | `[-0.0027,-0.0066,+0.1123] m` | 진단 초기값 |
| AFT ↔ `right_link_6` 회전 | zero | `[0,0,0]` | 유지 |
| AFT sensing origin ↔ `right_link_6` 이동 | zero | non-zero | 부호·plate 두께 미확정 |
| URDF hand subtree 질량 | `1.133 kg` | 공식 hand `1.3 kg` + adapter 미반영 | 향후 URDF 수정 필요 |
| payload gravity subtraction | `false` | validation 미통과 | `false` 유지 |

유효 식별값의 train-session 편차는 질량 10–90 percentile
`1.448~1.709 kg`, CoM z `+0.106~+0.124 m`였다. 따라서 `1.634 kg`/
`[-0.0027,-0.0066,+0.1123] m`는 설정에 즉시 쓰는 확정값이 아니라
물리+residual model의 초기 진단값이다.

이 물리 baseline을 고정 validation 3개에 적용한 force max/p95/RMSE는
`3.417/1.592/0.919 N`으로 `1 N` gate를 통과하지 못했다. 따라서
중력 model만 runtime에 승격하거나 `use_ft_payload_gravity_comp=true`로 바꾸지 않는다.

수정 대상과 적용 순서는 다음으로 고정한다.

1. 기존 dataset의 `right_tool_m2p1kg_oz0p17m_v1` ID와 원본은 소급 변경하지
   않는다. 향후 수집/runtime에만 확정된 질량·CoM으로 새 payload ID를 쓴다.
2. 제조사 URDF에 hand 질량·CoM, hand-side adapter 질량·CoM과
   sensor/adapter 이동 frame을 반영한다. 이 외부 URDF는 실제 치수를 확정한 뒤
   별도 변경으로 수정한다.
3. 이동 frame이 확정되면 observer의 `tip_to_sensor_translation_m`과 controller의
   F/T frame 계약을 같이 바꾸고 관련 테스트를 갱신한다. 회전 zero는 유지한다.
4. 완료: train13으로 물리 중력 baseline을 먼저 빼고 dynamic residual만 학습한 후
   기존 validation 3개로 다시 판정했다.
5. 사진의 wrist/hand cable은 당김이 AFT에 전달되지 않도록 slack과
   Velcro strain relief를 확인한다. 제조사 manual에 따라 센서 cable을 당기거나
   케이블타이로 강하게 묶지 않는다.

### 2026-08-21 물리 중력+dynamic residual 결과

- 실행 script: `scripts/run_physical_residual_validation.py`
- final report:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_train13_physical_residual_diagnostic_v4_20260821/validation_report.json`
- split manifest:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_train13_physical_residual_diagnostic_v4_20260821/split_manifest.json`
- scope: train13 식별·학습과 고정 validation3 선택만 사용; 과거 held-out test는
  load하지 않음

| 후보 | validation max [N] | p95 [N] | RMSE [N] | `1 N` gate |
|---|---:|---:|---:|---|
| 물리 중력 baseline | 3.417 | 1.592 | 0.919 | FAIL |
| physical residual dynamic MLP | 3.688 | 1.455 | 0.833 | FAIL |
| physical residual smoothed-dynamic MLP | **2.955** | 1.267 | 0.725 | FAIL |
| physical residual causal lag-2 smoothed MLP | 3.023 | **1.196** | **0.691** | FAIL |

max 우선 선택은 physical residual smoothed-dynamic MLP다. group별
max/p95/RMSE는 `div02` `2.936/1.446/0.811 N`, `div05`
`2.955/1.267/0.768 N`, `div07` `2.526/1.048/0.601 N`이다. 승인 model
bundle은 생성하지 않았다.

기존 train task 3개의 in-sample max/p95/RMSE는
`4.787/1.315/0.750 N`으로 max gate를 통과하지 못했다. 사후 +2 samples
정렬은 validation을 `2.955/1.267/0.725 → 2.616/1.200/0.694 N`으로
낮췄지만, 같은 정렬을 causal feature로 학습한 lag-2 후보의 max는
`3.023 N`으로 악화됐다. 공통 timestamp lag만으로 실패를 설명할 수 없다.

group 상수 offset oracle은 max/p95/RMSE를 `2.863/1.184/0.672 N`까지만
낮췄다. force error 상관은 speed `0.326`, acceleration `0.344`, 절대 sync error
`0.048`이었다. 가속도 하위/상위 사분위 max/p95는 각각
`1.889/0.933 N`, `2.955/1.503 N`이므로 동적 항이 주요 잔차에 기여한다.
정지 3구간의 median 대비 max는 `0.827~0.886 N`으로 과거 target-noise
관찰과 일관된다.

현재 max는 `1~3 N` 분기지만, targeted6가 이미 한 번의 추가 targeted
batch였고 train task in-sample도 실패했다. 따라서 같은 방식의 동작 data를
더 수집하지 않는다. cable strain-relief 조건은 고정됐고 adapter stack은 약
`30 mm` 참고치만 확보됐다. 다음 hardware 작업은 이 배치를 유지한 독립 정지
target-noise 재확인이다. 로봇 이동, zero-set, 수집은 별도 사용자 승인 전에
실행하지 않는다.

Target 반복성 관찰:

- cable strain-relief 고정 후 첫 유효 독립 tare인 `tare01_retry01`은 59.89초,
  AFT/observer topic 약 1000 Hz, active command 0회, invalid 0회였다. 관절
  peak-to-peak 최대는 `0.158 deg`로 정지 기준을 충족했다. force median은
  `[+0.09,+0.05,+0.19] N`, 축별 STD는 `[0.121,0.159,0.248] N`, force norm
  p95/p99/max는 `0.632/0.783/1.186 N`이었다. `1 N` 초과는 40/59,790 samples,
  최장 4 samples로 관찰돼 첫 60초 측정만으로도 max gate는 실패했다. 최초
  `tare01`은 2.59초 불완전 bag이므로 분석에서 제외한다.
- 두 번째 독립 tare `tare02`도 59.89초, active command 0회, invalid 0회,
  관절 peak-to-peak 최대 `0.164 deg`로 유효했다. force median은
  `[+0.03,-0.10,-0.03] N`, 축별 STD는 `[0.121,0.156,0.250] N`, force norm
  p95/p99/max는 `0.564/0.717/1.810 N`이었다. max는 약 20 ms 연속됐고 당시
  J2 속도는 `-0.0252 rad/s`였으므로 holding controller의 미세 보정이 동반됐지만
  사용자 command나 큰 자세 이동은 없었다. 실제 정지 운용 조건의 max gate는
  두 독립 tare에서 연속 실패했다.
- 세 번째 독립 tare `tare03`도 59.89초, active command 0회, invalid 0회,
  관절 peak-to-peak 최대 `0.066 deg`로 유효했다. force median은
  `[-0.12,-0.04,-0.57] N`, 축별 STD는 `[0.123,0.167,0.226] N`, force norm
  p95/p99/max는 `0.994/1.149/1.478 N`이었다. 세 tare의 force median 축별
  peak-to-peak는 `[0.21,0.15,0.76] N`, median vector 최대 거리는 `0.794 N`이다.
  세 run 모두 현행 축별 STD `<=0.40 N`은 통과했지만 force norm max는 각각
  `1.186/1.810/1.478 N`으로 `1 N` gate를 모두 실패했다. cable 고정 뒤에도
  실제 정지 target 자체가 strict `1 N max`를 보장하지 않으므로 같은 반복 tare나
  동작 data를 더 수집하지 않는다.
- force와 observer joint velocity를 timestamp 정렬하면 `tare02`의 `1.810 N`
  peak에는 `0.0252 rad/s` 미세 보정이 동반돼 impedance holding motion의 기여가
  가능하다. 그러나 `tare01_retry01`과 `tare03` peak의 속도 norm은 각각
  `0.000002/0.000000 rad/s`였고, speed `<=0.02 rad/s` sample만 보아도 각 run의
  max는 `1.186/1.173/1.478 N`이었다. force norm과 speed의 Pearson 상관도
  `0.078/0.126/0.045`로 낮다. 따라서 impedance controller 미세운동은 일부 peak의
  기여 요인이지만 전체 `1 N` 초과와 tare 중심 이동의 단독 원인은 아니다.
- 운영자는 impedance controller 종료 후에도 robot driver가 현재 자세를 유지한다고
  확인했다. 실제 controller OFF ROS graph에서는 `/contact_state/observer_input`은
  사라지고 `/dsr01/joint_states`가 계속 발행됐으며, 확인 자세는 기준 자세에서 모든
  축 `0.35 deg` 이내였다. controller OFF A/B에서는 AFT wrench와 이 joint-state
  topic을 함께 기록한다. 사용자가 controller OFF A/B의 새 zero-set과 기록을 승인했다.
- 승인 후 첫 controller OFF 비교 `off_tare01`을 59.75초 기록했다. 관절
  peak-to-peak 최대는 `0.00015 deg`, force median은 `[-0.09,+0.06,+0.06] N`,
  축별 STD는 `[0.118,0.170,0.244] N`, force norm p95/p99/max는
  `0.588/0.726/1.128 N`이었다. Fz 선형 변화는 `-0.078 N/min`으로 ON 3회의
  `-0.162~-0.211 N/min`보다 작았지만, 완전 정지에서도 max gate는 실패했다.
  controller가 motion과 일부 peak를 키울 수는 있어도 strict `1 N max` 실패의
  단독 원인은 아니라는 기존 판정을 지지한다. 독립 tare 비교를 위해 OFF 2회를
  추가했다.
- 두 번째 OFF 비교 `off_tare02`도 59.79초, 관절 peak-to-peak 최대
  `0.00015 deg`로 유효했다. force median은 `[-0.09,+0.12,0.00] N`, 축별
  STD는 `[0.123,0.171,0.258] N`, force norm p95/p99/max는
  `0.592/0.716/1.010 N`이었다. `1 N` 초과는 4/59,789 samples이고
  median-centered max는 `0.997 N`이다. OFF 두 tare의 median vector 거리는
  `0.085 N`으로 ON 세 tare의 최대 `0.794 N`보다 작다. 마지막 OFF 독립 tare로
  재현성을 확인했다.
- 세 번째 OFF 비교 `off_tare03`도 59.79초, 관절 peak-to-peak 최대
  `0.00015 deg`로 유효했다. force median은 `[+0.01,-0.03,+0.24] N`, 축별
  STD는 `[0.119,0.168,0.263] N`, force norm p95/p99/max는
  `0.695/0.849/1.231 N`이었다. OFF 3회의 max는 `1.128/1.010/1.231 N`으로
  모두 strict gate를 실패했다.
- ON/OFF 3:3 비교에서 joint peak-to-peak는 ON `0.066~0.164 deg`, OFF
  약 `0.00015 deg`; p99는 ON `0.717~1.149 N`, OFF `0.716~0.849 N`; run별
  max 중앙값은 ON `1.478 N`, OFF `1.128 N`; tare median vector 최대 거리는
  ON `0.794 N`, OFF `0.300 N`이었다. controller OFF가 기록된 자세 신호 안정성과
  typical/peak force를 개선하지만 `1 N max`를 보장하지는 않는다. Fz 선형 변화도
  ON `-0.211~-0.162 N/min`, OFF `-0.305~+0.036 N/min`으로 OFF에서 일관되게
  작지 않아 sensor/tare settling 성분이 별도로 남는다. 단, ON 관절값은 observer
  input, OFF 관절값은 driver joint-state에서 기록돼 joint peak-to-peak의 절대 배율은
  참고치다. 동일한 AFT topic의 force 비교는 직접 비교지만 독립 tare 3회씩인 제한도
  있으므로 controller의 정량 기여율로 해석하지 않는다. A/B 반복은 여기서 중단한다.
- OFF 각 run의 동일 길이 처음/마지막 20초를 비교하면 마지막 20초 p99/max는
  `0.672/0.966`, `0.716/1.001`, `0.738/1.020 N`이었다. 첫 20초 max
  `1.127/1.010/1.231 N`보다 모두 낮아 zero-set 후 3초 대기가 일부 tare의 settling을
  충분히 제외하지 못했을 가능성이 있다. 다만 20초 구간의 max 표본 수가 60초보다
  작다는 영향도 있어, controller OFF에서 zero-set 후 60초 대기하고 60초 기록하는
  별도 diagnostic으로 확인했다.
- 승인된 60초 post-zero settle diagnostic `off_settle60_tare01`은 controller OFF에서
  59.79초 기록했고 관절 peak-to-peak 최대 `0.00015 deg`였다. force median은
  `[-0.07,+0.09,-0.19] N`, 축별 STD는 `[0.126,0.170,0.238] N`, force norm
  p95/p99/max는 `0.616/0.749/1.184 N`이었다. `1 N` 초과는 20/59,789 samples,
  최장 2 samples였고 max는 기록 59.45초에 joint speed `0`에서 발생했다. 처음
  20초 max `0.989 N`과 달리 마지막 20초 max는 `1.184 N`이므로 settle을 60초로
  늘려도 strict `1 N max`는 보장되지 않았다. 추가 settle/tare 측정은 중단한다.
- 고정 manifest 16개 독립 tare의 같은 zero pose force median은 Fz에서
  `-0.565~+0.810 N`, peak-to-peak `1.375 N`, tare 간 STD `0.356 N`이었다.
- validation의 1초 이상 정지·관절변위 `<=0.2 deg`인 3구간에서 median 대비 force
  norm p95는 `0.551~0.591 N`, max는 `0.830~0.895 N`이었다.
- 이전 장기 정지 실험의 순간 max `1.327 N`, 독립 tare Fz peak-to-peak
  `1.31 N`과도 일관된다. target noise와 tare 반복성이 `1 N` max 예산의 상당 부분을
  차지하지만, group-offset oracle 결과상 현재 `2.955 N` 전체 실패의 유일한 원인은
  아니다.

### 2026-08-21 causal residual filter·robust gate offline 평가

- 실행 script: `scripts/run_causal_residual_filter_validation.py`
- 최종 report:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_train13_causal_residual_filter_diagnostic_v3_20260821/filter_validation_report.json`
- split manifest:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_train13_causal_residual_filter_diagnostic_v3_20260821/split_manifest.json`
- 입력 범위: train13 재학습, 고정 validation3, 이미 저장된 정지 bag 7개만 사용했다.
  과거 held-out test는 load하지 않았고 hardware, ROS graph, runtime 설정,
  acceptance contract는 변경하지 않았다.
- 모든 filter state는 session마다 reset했고 EMA와 causal median은 첫 sample로
  초기화했다. 약 1000 Hz 정지 bag은 bag timestamp 순서대로 262.5 Hz에 causal
  decimation해 별도 target-noise evidence로만 평가했다.

선택 physical residual smoothed-dynamic MLP의 unfiltered validation max는
`2.9550129366 N`으로 기존 report와 차이가 `1.28e-9 N`뿐이어서 재학습을
재현했다. validation3 aggregate 결과는 다음과 같다.

| causal filter | max [N] | p95 [N] | p99 [N] | RMSE [N] | `>1 N` 비율 | false contact 횟수 |
|---|---:|---:|---:|---:|---:|---:|
| raw | 2.955 | 1.267 | 1.665 | 0.725 | 13.59% | 6 |
| median-3 | 2.872 | 1.182 | 1.584 | 0.676 | 10.43% | 5 |
| median-5 | 2.798 | 1.112 | 1.492 | 0.636 | 8.02% | 5 |
| EMA 20 Hz | 2.353 | 1.038 | 1.383 | 0.595 | 5.99% | 1 |
| EMA 10 Hz | 1.912 | 0.908 | 1.229 | 0.523 | 3.09% | 0 |
| EMA 5 Hz | 1.612 | 0.808 | 1.104 | 0.465 | 1.63% | 0 |
| EMA 2.5 Hz | 1.514 | 0.751 | 1.000 | 0.429 | 1.00% | 0 |
| median-3 → EMA 20 Hz | 2.323 | 1.015 | 1.359 | 0.582 | 5.39% | 1 |

Gate는 aggregate뿐 아니라 validation 3개 각각이 모두 통과해야 PASS로
판정했다. strict `max<=1 N`과 `p99<=1 N`은 모든 후보가 실패했다. EMA 2.5 Hz의
aggregate p99는 `0.9999 N`이지만 div02 p99가 `1.224 N`이므로 FAIL이다.
EMA 5 Hz와 2.5 Hz만 `p95<=1 N`과 함께 `max<=2 N` 또는 현행
`2 N/8 ms` detector false-contact 0회 조건을 통과했다. 그중 지연이 작은
EMA 5 Hz가 최소 후보지만 div02의 p99는 `1.295 N`이고 `>1 N`이 최장
`522 ms` 지속됐다. 따라서 p95 gate는 이 지속 오차를 숨기며 아직 승인할 수 없다.

4 N step을 262.5 Hz에서 넣은 합성 응답에서 raw의 2 N threshold 도달/현행
8 ms hold 포함 contact-on은 `0/11.43 ms`, EMA 20 Hz는 `3.81/15.24 ms`,
EMA 10 Hz는 `7.62/19.05 ms`, EMA 5 Hz는 `19.05/30.48 ms`, EMA 2.5 Hz는
`41.90/53.33 ms`였다. 이는 합성 filter 지연일 뿐 실제 접촉 onset 검증을
대체하지 않는다.

정지 bag 7개 aggregate의 raw max/p95/p99는 `1.810/0.740/0.952 N`이었다.
raw p99는 `on_tare03` 때문에 every-group gate를 실패했고 EMA 20 Hz 이하부터
every-group p99 gate를 통과했다. EMA 2.5 Hz만 decimation된 정지 data의
strict max도 `0.974 N`으로 통과했지만, validation strict max는 `1.514 N`이고
합성 contact-on 지연도 `53.33 ms`이므로 runtime 후보로 승인하지 않는다.

결론적으로 causal filtering은 정지 target-noise와 false-contact를 줄이지만
validation의 저주파·지속 residual을 제거하지 못한다. 현행 strict gate는 그대로
유지하며 승인 model/filter는 없다. robust gate를 채택하려면 최소한 group별
통계, 별도 peak/false-contact 조건, 허용 contact onset latency를 acceptance
contract에서 함께 확정하고 새 held-out test는 그 결정 이후 한 번만 사용해야 한다.

### 2026-08-21 div02 지속 residual 원인 진단

- 실행 script: `scripts/run_div02_residual_diagnostic.py`
- 최종 report:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_train13_div02_residual_diagnostic_v3_20260821/div02_residual_report.json`
- 입력 범위: train13과 protected validation의 div02만 사용했다. 과거 held-out
  test, hardware, ROS graph, runtime/acceptance 설정은 사용하거나 변경하지 않았다.
- 누구·무엇: physical residual smoothed-dynamic MLP의 div02 EMA 5 Hz residual에서
  `>1 N`인 749/16,308 sample, 56개 연속 사건을 분석했다.
- 언제·어디: 최장 사건은 div02 정렬 시작 후 `20.171~20.693 s`, 138 sample,
  `521.90 ms`였다. AFT frame median force는
  `[-0.799,-0.671,-0.761] N`, norm median/max는 `1.344/1.612 N`이며
  median 기준 dominant axis는 Fx였다.
- 어떻게: train당 최대 5,000개 train-only smoothed-dynamic window로 24D
  표준화 nearest-neighbor 거리를 계산하고 posture/velocity/acceleration 성분을
  분리했다. sync·속도·가속도와 validation-fitted offset/drift/payload-mass oracle은
  원인 구분에만 사용했고 model 선택에는 사용하지 않았다. `task_error` 원본 단위는
  translation mm, rotation deg로 확인해 report에 명시했다.

| evidence | `>1 N` sample | `<=1 N` sample | 판정 |
|---|---:|---:|---|
| speed norm median | `0.170 rad/s` | `0.297 rad/s` | 고속 motion이 주원인 아님 |
| acceleration norm median | `1.821 rad/s²` | `1.539 rad/s²` | 약한 기여 가능 |
| absolute sync p95 | `1.155 ms` | `1.153 ms` | sync가 주원인 아님 |
| full 24D train-distance median | `2.847` | `2.125` | 학습범위 차이 있음 |
| posture distance median percentile | `88.6%` | - | posture 일반화 부족 기여 |
| velocity distance median percentile | `40.5%` | - | velocity OOD 아님 |
| acceleration distance median percentile | `63.7%` | - | 보조 기여 가능 |

다만 최장 522 ms 사건의 full/posture distance median은 `1.933/1.269`로
극단적인 OOD가 아니었고 speed median `0.084 rad/s`, sync p95 `0.596 ms`인
저속·정상 sync 구간에서도 지속 residual이 발생했다. 따라서 전체 사건의 posture
coverage 부족만으로 최장 사건을 설명할 수 없다. EMA를 2.5 Hz로 낮춰도 div02
p99 `1.224 N`, 최장 `700.95 ms`로 남아 고주파 target-noise도 주원인이 아니다.

Validation-fitted 진단 oracle 결과는 다음과 같다. 이는 validation leakage이므로
승인 model이나 보정값으로 사용할 수 없다.

| EMA 5 Hz 진단 | max [N] | p99 [N] | `>1 N` 최장 |
|---|---:|---:|---:|
| 보정 없음 | 1.612 | 1.295 | 521.9 ms |
| group constant offset oracle | 1.438 | 1.130 | 388.6 ms |
| time-linear offset+drift oracle | 1.376 | 1.070 | 270.5 ms |
| payload delta-mass oracle | 1.504 | 1.159 | 419.0 ms |
| payload delta-mass+offset oracle | 1.430 | 1.117 | 388.6 ms |

payload delta-mass fit은 `+0.058 kg`, force residual과 delta-g의 flattened cosine은
`0.236`으로 약했고 어느 oracle도 p99/지속 residual을 제거하지 못했다. 따라서
원인은 단순 sync, 속도, payload 질량, group offset/drift 하나가 아니라
`일부 posture의 train coverage/일반화 부족 + 느린 session-dependent bias 또는
hysteresis`의 복합 성분으로 판정한다. 후자의 실제 물리 원인은 현재 dataset에
온도·cable force·방향 이력 label이 없어 더 세분할 수 없다.

같은 q/dq/qdd와 짧은 history에 더 강한 low-pass를 적용하거나 p95 gate만 완화하는
것은 이 지속 오차를 숨길 뿐이다. 현재 모델/filter는 계속 미승인으로 두며, 다음
모델 변경을 하려면 train-only에서 pose coverage와 느린 bias/history를 직접 다루는
후보를 먼저 고정 validation3로 비교해야 한다.

### 2026-08-21 train-only pose/history correction 후보 결과

- 실행 script: `scripts/run_pose_history_candidate_validation.py`
- 최종 report:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_train13_pose_history_candidate_diagnostic_v2_20260821/pose_history_candidate_report.json`
- 입력 범위: train13 fit과 protected validation3 비교만 사용했다. held-out,
  hardware, runtime/acceptance 설정은 사용하거나 변경하지 않았다.
- 기존 physical residual smoothed-dynamic MLP는 유지하고 train session당 최대
  5,000 window의 in-sample residual을 64-neighbor 평균 correction으로 만들었다.
  posture 12D, current dynamic 24D, `0.484 s` slow-history 48D와 correction gain
  `0.25/0.5/1.0`만 사전 고정해 비교했다. 모든 후보는 같은 128-sample common
  validation window를 사용했다.

| 후보 | raw max/p99 [N] | EMA 5 Hz max/p99/RMSE [N] | div02 p99 [N] | `>1 N` 최장 |
|---|---:|---:|---:|---:|
| baseline | 2.955/1.666 | 1.612/1.104/0.465 | 1.295 | 521.9 ms |
| posture KNN ×0.25 | 2.963/1.681 | 1.613/1.102/0.473 | 1.297 | 461.0 ms |
| dynamic KNN ×0.25 | 2.999/1.677 | **1.588/1.087/0.470** | 1.276 | 461.0 ms |
| slow-history KNN ×0.25 | 2.994/1.677 | 1.591/1.091/0.467 | 1.277 | 457.1 ms |
| slow-history KNN ×0.5 | 3.050/1.690 | 1.596/1.090/0.479 | 1.265 | 453.3 ms |

dynamic ×0.25가 filtered aggregate p99를 `0.017 N`, slow-history 후보가 div02
지속시간을 최대 `68.6 ms` 줄였지만 raw max/p99와 filtered RMSE는 baseline보다
악화됐다. gain 1.0에서는 raw false-contact도 baseline 6회에서 dynamic 11회,
posture 12회, slow-history 8회로 늘었다. 모든 후보가 strict max와 every-group
p99 gate를 계속 실패했다.

따라서 단순 nearest-neighbor pose/history correction은 train residual을 validation
session으로 안정적으로 전이하지 못한다. 이 후보들은 runtime bundle로 만들지 않고
폐기한다. 현재 dataset에는 temperature, cable force, 정확한 time-since-zero 같은
느린 bias state가 없으므로 기존 label만으로 같은 후보를 더 튜닝하지 않는다.

### 2026-08-22 전체 offline 개선 sweep 결과

- 방법 원장: [`improve free space wernch model.md`](../improve%20free%20space%20wernch%20model.md)
- 실행 script: `scripts/run_free_space_improvement_sweep.py`
- 최종 report:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_train13_improvement_sweep_v2_20260822/improvement_sweep_report.json`
- 범위: train13과 development validation3만 사용했다. 과거 held-out/test, hardware,
  ROS graph, runtime과 acceptance contract는 사용하거나 변경하지 않았다.

data 제외, zero median 직접/학습 보정, session/motion sampling, Huber/MAE/force-tail
loss, ridge, MLP 크기, multi-scale history, GRU32, motion-regime experts, seed
ensemble, quadratic/random-Fourier feature와 causal filter를 순서대로 비교했다.

현재 offline 최선은 physical gravity를 먼저 빼고 다음 54D causal feature에 L2
regularization `1.0`인 multi-output ridge를 적합한 후보이다.

```text
sin(q)[6], cos(q)[6], current dq[6],
mean dq over 8/16/32 samples[18],
mean qdd over 8/16/32 samples[18]
```

seed 8 validation max/p95/RMSE는 `2.162/1.135/0.666 N`이며 기준 physical
residual MLP의 `2.955/1.268/0.726 N`보다 각각 약 `26.9/10.5/8.3%`
개선됐다. group max는 div02/div05/div07이 `2.145/2.162/2.070 N`이다.
seed 7/8/9 max도 `2.172/2.162/2.178 N`으로 안정적이었다. train13 내부
13-fold leave-one-zero-group-out에서도 base ridge보다 p95/RMSE와 group max
중앙값이 개선돼 validation3에만 국한된 방향은 아니었다.

다만 strict max `1 N`은 계속 실패했다. 최선 ridge residual에 EMA 20/10/5/2.5 Hz를
적용한 max도 `1.850/1.665/1.544/1.444 N`으로 모두 실패했고 낮은 cutoff일수록
`>1 N` 지속시간이 늘었다. 이 후보는 offline 최선으로만 선택하고 runtime filter나
승인 상태는 변경하지 않는다.

### 2026-08-22 최종 ridge diagnostic bundle과 inference benchmark

- 실행 script: `scripts/finalize_free_space_ridge_bundle.py`
- 최종 artifact:
  `/home/vision/.ros/ft_fb_leaderarm/models/right_train13_ridge_short_multiscale_bundle_v3_20260822`
- 범위: train13 fit과 development validation3만 사용했다. 과거 held-out/test,
  hardware, ROS graph와 runtime 설정은 사용하거나 변경하지 않았다.
- runtime-compatible 경로: 54D short-multiscale projection + TorchScript ridge residual
  + Pinocchio payload gravity
- runtime/offline 전체 validation prediction 최대 차이: `0.000206 N`
- runtime validation max/p95/RMSE: `2.162/1.135/0.666 N`
- 2,000회 model-only latency mean/p99/max:
  `0.05463/0.08783/0.60003 ms`
- 등가 처리율 mean/p99/최악: 약 `18,304/11,385/1,667 Hz`
- `FS-04` timing gate: PASS (`p99<=3.048 ms`, `max<=3.810 ms`)
- `FS-03` accuracy gate: FAIL이므로 metadata는 `approved=false`다. 승인 bundle만 받는
  observer loader가 이 artifact를 거부하는 것도 확인했다.
- model SHA-256:
  `8c61261bb2fdd0151291f9c52ca627e59e04d71bc5655c92df5081943280ee8b`
- finalizer SHA-256:
  `7a9ae646cab25fe156ada50e728a5d2ec516773664263b8b38df3ad0420e8ff8`

따라서 최종 ridge의 계산 시간은 262.5 Hz 요구에 충분하다. 남은 병목은 계산 주기가
아니라 `max<=1 N` 정확도다. observer 실행과 ROS 유효 publish rate `FS-05` 측정은
승인 상태를 우회해야 하므로 진행하지 않는다.

현재 판정은 체크포인트의 `1 N < max < 3 N` 분기다. group residual review와
causal lag-2 평가를 완료했고 targeted6가 이미 한 번의 추가 targeted batch였으므로
같은 방식의 동작 episode 수집은 중단한다. adapter/cable 계약 고정과 독립 정지
반복 evidence 3회도 완료했고 target noise 자체가 strict `1 N max`를 통과하지
못했다. 다음 단계는 acceptance 기준을 유지할지, robust statistic과 별도 peak
안전 기준으로 변경할지 명시적으로 결정하는 것이다. 합격 기준은 evidence 없이
완화하지 않는다.

## 다음 session 재개 체크포인트

- targeted6 수집과 고정 validation 분석은 완료했다.
- 현재 validation offline 최선은 physical gravity + 32-sample multi-scale ridge
  residual max `2.162 N`; 승인 모델은 없다.
- 최종 ridge diagnostic bundle의 model-only p99/max는 `0.08783/0.60003 ms`로
  262.5 Hz timing gate를 통과했다. accuracy 실패로 `approved=false`이며 observer에는
  연결하지 않는다.
- 추가 동작 data, 새 held-out test, observer-only, contact와 feedback 단계는 금지한다.
- 공식 hand `1.3 kg`, train13 유효 payload `1.634 kg`, AFT-frame 유효 CoM
  `[-0.0027,-0.0066,+0.1123] m`를 진단 초기값으로 기록했다.
- AFT와 `right_link_6` 회전은 zero로 확인했지만 원점 이동과 adapter 실제
  질량·CoM은 미확정이다. 현재 controller의 payload gravity subtraction은 계속 꺼 둔다.
- 물리 baseline validation은 `3.417/1.592/0.919 N`, 최선 residual model은
  `2.955/1.267/0.725 N` max/p95/RMSE로 모두 실패했다.
- residual review와 causal lag-2 평가는 완료했고 과거 test는 읽지 않았다.
- 같은 방식의 동작 data를 더 수집하지 않는다.
- cable slack/Velcro strain relief는 완료했다. 전체 cable 경로 사진은 필수로
  요구하지 않는다.
- adapter 모델·질량은 미확인이고 stack 약 `30 mm`는 참고치이므로 URDF/runtime
  translation과 payload gravity subtraction은 수정하지 않는다.
- 다음 hardware 단계는 현재 cable 배치를 건드리지 않은 독립 tare 정지 noise
  측정이며 3/3 완료했다. 세 run의 force norm max `1.186/1.810/1.478 N`으로
  target noise가 strict `1 N max`를 모두 실패했다. 같은 방식의 동작 data나
  반복 tare를 더 수집하지 않는다.
- impedance controller OFF A/B 정지 측정도 3/3 완료했다. OFF에서 기록된 joint와
  force p99/max는 개선됐지만 max `1.128/1.010/1.231 N`으로 strict gate를 모두
  실패했다. 추가 A/B tare는 수집하지 않는다.
- OFF run의 마지막 20초 max가 `0.966/1.001/1.020 N`으로 수렴해 zero-set 후
  3초 settle 계약이 짧을 가능성을 확인하기 위해 60초 settle diagnostic도 완료했다.
  결과 max `1.184 N`으로 실패했으며 마지막 20초에 peak가 발생했다. settle 연장으로
  strict gate를 해결할 수 없으므로 추가 settle/tare 측정은 하지 않는다.
- `1 N max` gate를 바꾸려면 `docs/acceptance-contract.md`의 기준과 평가 방법을
  같은 변경에서 명시적으로 승인해야 한다.
- causal filter offline 평가는 완료했다. EMA 5 Hz만 validation3 모든 group에서
  `p95<=1 N + max<=2 N/false-contact 0회` 후보 gate를 통과했지만 div02 p99
  `1.295 N`, `>1 N` 최장 `522 ms`, 합성 contact-on `30.48 ms`이므로 승인하지
  않았다. 모든 후보가 strict max와 every-group p99 gate는 실패했다.
- filter와 robust gate는 runtime/config/acceptance contract에 반영하지 않았다.
  실제 허용 onset latency와 robust gate 정의를 먼저 명시적으로 결정해야 한다.
- div02 지속 residual 진단도 완료했다. 전체 `>1 N` sample에는 posture coverage
  부족이 기여했지만 최장 522 ms 사건은 학습범위 안의 저속·정상 sync에서도
  발생했다. 단순 sync/payload/offset/drift 원인이 아니며 pose 일반화 오차와
  느린 session bias/hysteresis의 복합 성분으로 판정했다.
- train-only posture/dynamic/0.484초 history KNN correction도 모두 실패했다.
  filtered p99/지속시간은 소폭 개선됐지만 raw error와 RMSE가 악화돼 후보 bundle을
  생성하지 않았고, 기존 label로 추가 KNN tuning은 중단한다.
- 현재 데이터로 정당하게 평가 가능한 model/data/feature/loss/ensemble/filter 방법은
  2026-08-22 improvement sweep에서 모두 비교했다. 32-sample multi-scale ridge가
  기준 max를 `2.955→2.162 N`으로 줄였지만 strict gate는 실패했다. temperature,
  정확한 time-since-zero, cable force와 정확한 adapter/frame 물리량은 dataset에 없어
  추가 입력으로 평가할 수 없다.

다음 대화에서 사용할 재개 문구:

```text
FT-20260819-01의 "다음 session 재개 체크포인트"부터 이어서 진행해줘.
targeted6는 6/6 완료됐고 offline improvement sweep 최선 max는 2.162 N이야.
독립 정지 target-noise 3/3도 max 1.186/1.810/1.478 N으로 실패했어.
controller OFF A/B 3/3도 개선은 있었지만 max 1.128/1.010/1.231 N으로 실패했어.
60초 post-zero settle도 max 1.184 N으로 실패했어.
causal filter offline 평가도 strict max와 every-group p99는 전 후보 실패했어.
EMA 5 Hz의 every-group p95+peak/false-contact gate만 후보 PASS지만 div02의
p99 1.295 N, >1 N 최장 522 ms, 합성 contact-on 30.48 ms라 미승인이야.
div02 진단상 posture 일반화 부족과 느린 session bias/hysteresis가 함께 남았고
sync, 속도, payload 질량, 단순 offset/drift 하나로는 설명되지 않았어.
train-only posture/dynamic/0.484초 history KNN correction도 raw error를 악화시켜
전 후보 미승인이야. 기존 label로 같은 후보 tuning은 중단했어.
전체 improvement sweep에서는 physical gravity + 32-sample multi-scale ridge가
max/p95/RMSE 2.162/1.135/0.666 N으로 최선이지만 strict 1 N은 실패했어.
최종 ridge의 2,000회 model-only p99/max는 0.08783/0.60003 ms로 timing gate는
통과했지만 approved=false라 observer/runtime에는 연결하지 않았어.
허용 onset latency와 robust gate 정의 결정부터 진행해줘.
```

## 재검증

- dataset validation: PASS
- validation 최대 force-vector 오차 `<= 1 N`: FAIL (`3.980 N`)
- held-out test 최대 force-vector 오차 `<= 1 N`: FAIL (`6.260 N`)
- 새 단일 모델 validation 최선 max: FAIL (`3.947 N`)
- 단순 ensemble validation 최선 max: FAIL (`3.530 N`)
- targeted6 train13 고정 validation 최선 max: FAIL (`3.201 N`)
- train13 물리 중력 baseline max: FAIL (`3.417 N`)
- train13 physical residual smoothed-dynamic max: FAIL (`2.955 N`)
- train13 physical residual causal lag-2 max: FAIL (`3.023 N`)
- causal lag-2 qdd-smoothed 후보 max: FAIL (`3.539 N`)
- cable 고정 후 독립 정지 target-noise max: FAIL
  (`1.186/1.810/1.478 N`, 3/3)
- impedance controller OFF 정지 target-noise max: FAIL
  (`1.128/1.010/1.231 N`, 3/3)
- impedance controller OFF + post-zero 60초 settle max: FAIL (`1.184 N`)
- causal filter validation strict max/every-group p99: FAIL (전 후보)
- EMA 5 Hz validation every-group p95 + peak/false-contact 후보 gate: PASS
  (후보 평가만 통과; runtime/acceptance 미승인)
- div02 지속 residual 단일원인 제거: FAIL
  (posture coverage + 느린 session bias/hysteresis 복합 성분)
- train-only pose/history KNN correction: FAIL
  (filtered 일부 개선, raw max/p99·RMSE 악화)
- 전체 offline improvement sweep 최선: FAIL
  (physical gravity + 32-sample multi-scale ridge,
  validation max/p95/RMSE `2.162/1.135/0.666 N`)
- 최선 ridge + causal filter strict max: FAIL
  (EMA 20/10/5/2.5 Hz max `1.850/1.665/1.544/1.444 N`)
- 과거 held-out test 미로드: PASS
- CPU model-only inference p99/최악 등가 처리율: PASS
  (기존 final13 MLP 약 `41,900/32,400 Hz`, `0.02388/0.03088 ms`)
- 최종 ridge runtime-compatible model-only inference: PASS
  (p99/최악 약 `11,385/1,667 Hz`, `0.08783/0.60003 ms`; 2,000회)
- 최종 ridge observer 유효 publish rate: 미실행
  (`approved=false`를 우회하지 않음)
- final status: OPEN; target noise가 strict `1 N max`를 실패해 acceptance 기준 결정 전
  runtime 사용 금지
